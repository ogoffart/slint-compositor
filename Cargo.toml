[workspace]
resolver = "2"
members = [
    "crates/slick",
    "crates/slick-wayland",
    "crates/slick-render",
    "crates/slick-shell",
    "crates/slick-tray",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/ogoffart/slick"

[workspace.dependencies]
slick-wayland = { path = "crates/slick-wayland" }
slick-render = { path = "crates/slick-render" }
slick-shell = { path = "crates/slick-shell" }
slick-tray = { path = "crates/slick-tray" }

slint = { version = "1.16", default-features = false, features = [
    "compat-1-2",
    "std",
    "renderer-skia-opengl",
    "backend-winit",
] }
slint-build = "1.16"

smithay = { version = "0.7", default-features = false, features = [
    "wayland_frontend",
    "desktop",
    "renderer_gl",
    "backend_egl",
] }

calloop = "0.14"
glow = "0.17"
log = "0.4"
env_logger = "0.11"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
anyhow = "1"
